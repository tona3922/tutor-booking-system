server:
  port: 8080

spring:
  application:
    name: booking-service
  datasource:
    url: jdbc:postgresql://localhost:5433/bookingdb
    driver-class-name: org.postgresql.Driver
    username: tommyvo
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  jwt:
    # dev-only secret; override via APP_JWT_SECRET env var for any real deployment
    secret: "dev-only-please-change-this-secret-key-32bytes-min!!"
    expiration-days: 7
